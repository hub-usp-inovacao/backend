Hub USP Inovação: Aviso sobre atualização de Empresas

As seguintes empresas solicitaram alterações nos seus dados:

<% @companies.each do |company| %>
  Nome da empresa: <%= company.name %>
  CNPJ: <%= company.cnpj %>
  Sócios:
  <% company.partners_values.each_with_index do |partner, i| %>
      Sócio <%= (i + 1) %>:
        Nome: <%= partner[:name] %>
        Email: <%= partner[:email] %>
        Vínculo: <%= partner[:bond] %>
        Telefone: <%= partner[:phone] unless !partner.key?(:phone) %>
        NUSP: <%= partner[:nusp] unless !partner.key?(:nusp) %>
        Unidade: <%= partner[:unity] unless !partner.key?(:unity) %>
  <% end %>
  Dados da empresa para atualizar:
  <% company.company_values.each do |hash| %>
    &emsp;<%= "#{hash.keys[0]}:" %> <%= "#{hash[hash.keys[0]]}" %>
  <% end %>

<% end %>